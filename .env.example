# PUBLIC — inlined into the JS bundle at build time. Assume the world reads it.
# This is the ENTIRE list on purpose: no Stripe publishable key (we redirect
# via session.url), no Supabase URL/key (the backend returns absolute URLs).
# NEVER put a secret behind a VITE_ prefix.
VITE_API_BASE_URL=/api
